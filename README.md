# sort-it
A help desk support backend platform that enables end users to raise error tickets for the IT support team to resolve. Built with Python and FastAPI. The platform exposes a RESTful API over HTTP.
